# Gate G1 — HQ / ERP contract

Date / attendees: 2026-07-30 / anh + em  
Sponsor decision: **Go (partial)** — contract scope đã chốt, pilot branch còn thiếu

## Questions for anh

1. HQ receiver = NAS custom API hay ART-ERP-BE?
2. Event → log only / attendance / CRM VIP / mix?
3. Enrollment embedding ở đâu?
4. Gallery theo branch hay whole chain?
5. Pilot branch + RTSP mẫu?

## Answers

1. **BE API**
2. **Điểm danh, chấm công; đếm người theo khu vực, heatmap; nhận diện khách lạ quen, VIP**
3. **Vector sinh trắc học khuôn mặt lưu ở BE, clone về Edge để Edge xử lý local; unknown vẫn phải ghi nhận để manual mapping trên ERP theo branch**
4. **Cả chuỗi có phân quyền chi nhánh**
5. **Chưa có pilot branch / RTSP mẫu**

## Changes requested

- Bổ sung event `unknown_face` để BE/ERP manual map
- Payload thêm bbox normalized để phục vụ zone counting / heatmap
- Thiết kế gallery sync theo RBAC chi nhánh nhưng nguồn dữ liệu tập trung ở BE

## Next step unlocked

Sau **Go partial**:

- Tiếp tục hoàn thiện API contract / data model BE
- Giữ block phần pilot performance / camera tuning đến khi có branch mẫu
